//! `stingstream-relay`: Docker image + binary bundling `iroh-relay` and `iroh-dns-server`
//! (pkarr discovery), with a `storage-node` compose profile that adds a full StingStream node.
//!
//! M0 skeleton stub, so the `mesh` Rust workspace builds cleanly on a clean clone. Implemented in
//! M3. See docs/ARCHITECTURE.md.

fn main() {
    println!("stingstream-relay: M0 skeleton stub, not yet implemented (see M3 in docs/ARCHITECTURE.md)");
}
