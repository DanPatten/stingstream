# deploy/

Placeholder. This directory will hold Dockerfiles, Docker Compose files (including the
`storage-node` profile for `stingstream-relay`), and platform installers (Windows winget/MSIX,
macOS pkg, Linux deb/AppImage) as they land in M3 (relay) and M8 (packaging). See
`docs/ARCHITECTURE.md` for the milestone plan.
