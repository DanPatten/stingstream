/**
 * `/manage` — the URL for Manage. See `(search)/search.tsx` for why these files
 * exist; a group's `index` is `/`, so a section without one has no address.
 */
export { default } from "./index";
