# @stingstream/api-client

Placeholder. This package will hold a TypeScript client generated from the StingStream OpenAPI
spec exposed by `StingStream.Core` at `/stingstream/api/v1/openapi.json` (see M1 in
`docs/ARCHITECTURE.md`). Generation is wired up in M2 alongside `packages/api-client` consumers in
`apps/stingstream`.
